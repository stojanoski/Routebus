USE [RouteBus]
GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE TABLE [dbo].[Admin](
	[Id] [int] NOT NULL,
	[Ime] [nvarchar](50) NULL,
	[Prezime] [nvarchar](50) NULL,
	[userName] [nvarchar](50) NULL,
	[pass] [nvarchar](50) NULL,
	[role] [nvarchar](15) NULL,
 CONSTRAINT [PK_Admin] PRIMARY KEY CLUSTERED 
(
	[Id] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]

GO



USE [RouteBus]
GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE TABLE [dbo].[Postojki](
	[Id] [int] NOT NULL,
	[Ime] [nvarchar](50) NULL,
	[Adresa] [nvarchar](70) NULL,
	[Lokacija] [nvarchar](50) NULL,
	[gMapsCoords] [nvarchar](90) NULL,
 CONSTRAINT [PK_Postojki] PRIMARY KEY CLUSTERED 
(
	[Id] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]

GO


USE [RouteBus]
GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE TABLE [dbo].[Prevoznik](
	[Id] [int] NOT NULL,
	[Ime] [nvarchar](50) NULL,
	[Linii] [nvarchar](50) NULL,
 CONSTRAINT [PK_Prevoznik] PRIMARY KEY CLUSTERED 
(
	[Id] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]

GO

USE [RouteBus]
GO


SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE TABLE [dbo].[Ruti](
	[Id_Postojka] [int] NOT NULL,
	[Id_Prevoznik] [int] NOT NULL,
	[Start] [nvarchar](20) NULL,
	[End] [nvarchar](20) NULL,
 CONSTRAINT [PK_Ruti] PRIMARY KEY CLUSTERED 
(
	[Id_Postojka] ASC,
	[Id_Prevoznik] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]

GO

ALTER TABLE [dbo].[Ruti]  WITH CHECK ADD  CONSTRAINT [FK_Ruti_Postojki] FOREIGN KEY([Id_Postojka])
REFERENCES [dbo].[Postojki] ([Id])
ON UPDATE CASCADE
ON DELETE CASCADE
GO

ALTER TABLE [dbo].[Ruti] CHECK CONSTRAINT [FK_Ruti_Postojki]
GO

ALTER TABLE [dbo].[Ruti]  WITH CHECK ADD  CONSTRAINT [FK_Ruti_Prevoznik] FOREIGN KEY([Id_Prevoznik])
REFERENCES [dbo].[Prevoznik] ([Id])
ON UPDATE CASCADE
ON DELETE CASCADE
GO

ALTER TABLE [dbo].[Ruti] CHECK CONSTRAINT [FK_Ruti_Prevoznik]
GO